/**
 * review.js
 * Server-side validation for reviews
 */
const Validator = require('validator');

module.exports = (data) => {
  // validate input
    // check that course id exists
    // check that semester is one of 'january', 'spring', 'summer'
    // check that year is between 2018 and present
    // check that rating is between 1 and 5
    // check that difficulty is between 1 and 5
    // check that workload is above 1
    // check that body is not empty
  let errors = {};




  
}
